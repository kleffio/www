# HTTP Server Configuration
HTTP_ADDR=:8080

# Authentik Configuration (Required)
# For local development with Authentik in Docker: http://authentik-server:9000
# For production: https://auth.yourdomain.com
AUTHENTIK_BASE_URL=http://authentik:9000

# Get this token from: Authentik Admin UI → Directory → Tokens & App passwords
# Click "Create" and select "API" as the intent
AUTHENTIK_TOKEN=

# Cache Configuration
# How long to cache user data before refreshing from Authentik
# Format: duration string (e.g., 300s, 5m, 1h, 24h)
CACHE_TTL=5m

# Redis Configuration (Optional)
# Leave REDIS_ADDR empty to use in-memory cache (good for development/single instance)
# Set REDIS_ADDR for production with multiple instances
REDIS_ADDR=redis:6379
REDIS_PASSWORD=
REDIS_DB=0

# Docker Compose Notes:
# - If Authentik is in the same Docker network, use the service name
# - If Authentik is external, use the full URL (https://auth.yourdomain.com)
# - In-memory cache works fine for single-instance deployments
# - Use Redis for multi-instance/production deployments